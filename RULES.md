# 跨时代名将沙盘规则

`engine.js` 是无外部依赖的确定性模拟核心。浏览器脚本加载后提供
`globalThis.WarEngine`，Node 环境使用 `require('./engine.js')` 得到同一个对象。

## 输入地图

```js
{
  width: 1000,
  height: 700,
  regions: [{
    id: 0, name: '区域名', x: 40, y: 60,
    neighbors: [1],
    terrain: 'plain', // plain | mountain | river
    fertility: 1.0,   // 0.8 - 1.3
  }]
}
```

区域 id 必须从 0 连续到 `regions.length - 1`；`neighbors` 应表达相邻区域，且实际地图应保持双向。引擎会拒绝无效 id、地形和资源参数。地图必须有足够的区域，为本局选中的 3–12 位人物安排起点。世界图额外加入当地驻守人物，并为全部可争夺地盘设置归属；`seaLinks` 单独表示跨海路线，与陆地 `neighbors` 分开。远征地点不作为主地图邻接边。

## 一局游戏

```js
const state = WarEngine.createGame(map, 'seed-001', {
  factionIds: ['caocao', 'liubei', 'sunquan'],
  coalitions: true,
  expeditions: true,
});
WarEngine.step(state, map); // 每次调用推进一个月，允许原地更新 state
const rows = WarEngine.ranking(state, map);
WarEngine.assertInvariants(state, map);
```

`createGame` 选择 3–12 位人物；省略时使用原班十将。区域地图安排分散起点，其余地盘中立；世界地图将中国分给自选人物，域外地区分给固定驻守势力。主线默认为 `campaignPhase: 'domestic'`，发展与征战自动推进，但只在中国区域内交锋。

## 逐鹿、息兵与域外副本

```js
WarEngine.setCampaignPhase(state, 'truce');    // 天下息兵
WarEngine.setCampaignPhase(state, 'domestic'); // 再启烽烟
WarEngine.setCampaignPhase(state, 'world');    // 息兵后开启域外副本
```

- `domestic`：中国诸侯可交锋、归顺和合纵，域外守土。没有三家门槛或自动三家弹窗。
- `truce`：各方各守其土，推进时间仍会发展兵粮。已在途任务继续结算。
- `world`：国内诸侯保持会盟，沿陆路与海路收服域外；当地势力会应战。共同收服域外即达成副本目标，无需国内先归一家。

中国归于一家后自动休养，记入 `domesticWinner`，仍可开启域外副本。区域图沿用本图统一的胜利条件。历史关系、合纵和 AI 任务不能越过当前篇章的地域与休战限制。

以下经济与战斗阶段用于控制推演节奏，与上述篇章状态分开：

- `development`：第 1–12 月，以屯田、城防和征募为主。
- `war`：第 13–84 月，边境进攻逐渐增多。
- `attrition`：第 85–360 月，粮耗、战疲和士气差距会放大，强势相邻势力有机会通过劝降接管孤立区域。
- `decisive`：第 361 月起，战疲和补给压力继续累积，优势方更容易发起决定性攻势。
- `victory`：一方占据全部区域后停止。

## 战斗

进攻永远从己方区域出发，只能选择陆地 `neighbors` 或明确的 `seaLinks` 中的目标；进攻军队必须留下守备兵。战斗力量由出兵数、武力、统帅、谋略、守御、发展度、粮食、士气、战疲和地形共同决定。山地与河流提高守方力量，攻击者承受相应地形折减。

胜负由双方有效力量比较得出。种子用于开局分配、目标并列排序、有限的战场条件及计谋/归顺的概率判断，不会直接抽取最终胜者。失败会产生进攻损失、撤回部分残兵和守方损失；胜利会真实扣除双方伤亡、降低城防并转移 `owner`。`battle` 事件记录战斗与伤亡，成功后紧接着有一个 `capture` 事件。

每月军队消耗粮食；粮荒会造成兵力损失、士气下降和战疲上升。发展度提高收入，建设同时小幅修复城防；统御和发展等属性影响征募与经济效率。晚期相邻且明显占优的势力可以触发基于实力、民心和军略的劝降，仍然需要满足相邻条件，不固定归属。

## 固定人物能力与关系

完整数值表和关系表见 [CHARACTERS.md](CHARACTERS.md)。每人七维基础值固定且范围为 1–100；`ATTRIBUTES` 与 `RELATIONSHIPS` 提供给界面展示。各维度参与实际推演，角色身份不因个人最高分是哪一项而改变。

人物关系见完整数据表。主动整势力归顺只面向明确的旧主关系，例如徐达、常遇春归顺朱元璋，关羽、张飞、赵云和诸葛亮归顺刘备，周瑜归顺孙权。只有双方都入选本局时，关系才会影响推演。双方须存活独立、领土接壤，旧主须具备相应的实力、魅力与粮草；归顺不是开局必发事件。同阵营与旧恩降低相互进攻倾向，宿敌提高对对方的边境进攻倾向，不额外产生自动主从关系。

归顺通过 `lordId` 保留人物归属，合并其全部地盘、兵力、粮草与属臣。该人物结束独立领军，但仍可作为麾下人才增强团队能力。`effectiveStats(state, id)` 单独派生团队增益，不改写人物基础值。主从图不得有循环；角色归顺与战败失地分别记录，不能混为阵亡。

计谋事件 `tactic` 记录施计者、守方、地点、成功或识破、概率、相关能力、粮耗和防御效果。谋略与智力决定用计和防计表现，单次用计有粮耗与冷却。成功只调整该场守方有效战力，不保证攻占。计谋后可争取从刚攻占地盘向下一个相邻目标追击一次，仍须满足出兵和胜负条件，不能跨图吞并。

## 临时合纵

`state.coalitions` 保存由局势形成的盟约，与人物固定的历史关系分开。存在共同强敌时，符合接壤和强弱条件的两方可概率结盟。盟内互不进攻，进攻选择倾向共同目标，并有实际协同效果；战报记录缔盟与配合行动。

盟约有期限。势力覆灭、归顺、共同威胁缓解或期限届满会解除盟约，并经过冷却才可再次形成。只剩两方时不能借永久联盟锁死统一结局。是否启用合纵可在开局设置中选择。

## 远征

每张图配置 `expeditionSites`，包含地点、对手类型、说明、行程、难度和奖励。地方联盟、边地城邦及十字军营地等为跨时代游戏对手，不宣称是真实历史同场事件。

```js
const plan = WarEngine.canStartExpedition(state, 'caocao', siteId);
if (plan.allowed) WarEngine.startExpedition(state, 'caocao', siteId);
```

只能为当前篇章允许出征、仍独立存活的势力派遣部队。出征先从一处己方地块扣除兵员，并保留本土守军；军粮包括启程支出与行军期间的补给。每方同时最多一支在外远征军。粮草、兵力不足，地点冷却尚未结束或本局已结束时，不能出征。

`state.expeditions` 记录启程月、归期、出兵、耗粮、进度与战果。到期后由固定能力、补给、对手难度和种子结算胜败；存活兵员返回己方地盘，胜利带回地点奖励，失败仍会产生伤亡。远征不会绕过主地图的邻接规则，直接把远方领土并入主战场。所属势力退出独立领军后，尚未归来的远征也会中止。

「军议 · 远征」可选择主将与路线。页面明确展示成本、行程、难度和凯旋奖励。安排出征后继续推演，时间才会推进；允许参战的 AI 也会在条件合适时自行出征。

## 状态与事件

状态包含 `participantIds`、`options`、`regions`、`factions`、`coalitions`、`expeditions`、`events`、`lastEvents`、`winner` 和 `finished`。名册与排名只包括本局出场人物，图鉴仍可查看完整人物池。兵力与粮草都是用于娱乐推演的抽象单位，不代表现实人数或吨数。区域资源 `troops`、`development`、`fort` 与势力 `grain` 始终非负；每个区域只能有一个合法势力或 `null` 中立。`lastEvents` 只保存当前月事件，`events` 保存完整历史。

事件类型包括 `develop`、`battle`、`capture`、`tactic`、`allegiance`、`eliminate`、`victory`、`coalition`、`expedition` 和 `system`。事件面向 UI 的 `text` 和 `details` 使用中文；有 `from`/`to` 的战斗或占领事件必定对应地图相邻区域。`assertInvariants` 会检查领地数、合法 owner、资源、事件引用和终局一致性。UI 可将 `phase` 映射为 `development=休养生息`、`war=群雄交锋`、`attrition=长战消耗`、`decisive=决胜之势`、`victory=一统天下`。

## 确定性

同一 `map`、出场名单、玩法选项和 `seed`，以及相同月份的息兵、复战、副本选择和手动远征操作和 `step` 次数会得到相同状态和事件序列。改变种子会改变起点分配、战场条件与并列选择，因此不同种子可以产生不同的胜者；模拟不预设某一位名将必胜。
